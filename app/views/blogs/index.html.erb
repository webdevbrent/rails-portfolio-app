<%= render 'shared/blog_featured' %>

<div class="col-md-8 blog-main">
    <h3 class="pb-3 mb-4 font-italic border-bottom">
      From the Firehose
    </h3>
    <%= paginate @blogs %>
    <%= render @blogs %>
    <%= paginate @blogs %>
</div>



